作用：确保一个类只有一个实例并提供一个全局访问点

适用性：	
	1.类必须有一个实例，并且客户端必须可以从全局的接入点访问。
	2.当唯一的实例通过子类扩展时，并且客户端可以在不修改代码的情况下使用扩展的实例。


利弊：
     利：
	1.对单例访问控制，因为单例封装在内部，它可以对其进行严格的管理。
	2.减少名称空间。单例是对全局变量的改进，避免了全局污染。
	4.允许可变数量实例。单例模式可以控制类的实例个数。
